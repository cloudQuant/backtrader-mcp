# 迭代 009：流式数据管道与存储效率验收

## 验收项

| 编号 | 检查 | 期望证据 |
| --- | --- | --- |
| A1 | 流式注册 | 规范内容与旧实现逐字节一致（既有数据集哈希断言 + 14 格矩阵）；源变更检测语义不变。 |
| A2 | 源级去重 | 二次注册跳过解析、feed_objects 复用。 |
| A3 | 对齐与派生 | overlap 数值一致；坏值带行号报错；输出上限生效。 |
| A4 | refresh 增量 | 无变更零重哈希；陈旧条目清除；单事务替换。 |
| A5 | 连接治理 | ResourceWarning 归零；watchdog 线程并发正确。 |
| A6 | 存储 GC | 三种 clean 引用完整性与审计。 |
| A7 | 静态质量 | ruff/format/mypy 通过。 |
| A8 | 全量回归 | pytest ≥80% 覆盖率。 |
| A9 | 清洁验收 | 协议测试 + 14 格矩阵 status=passed。 |

## 已记录证据

- **A1**：`test_streamed_registration_is_byte_identical_to_legacy` 以旧算法
  重建参考输出，断言流式注册的 CAS 内容逐字节一致、内容哈希与行数相同；
  全量数据契约测试与 14 格矩阵（真实子进程回测）全部通过。
- **A2**：`test_second_registration_reuses_source_level_dedup` 断言二次注册
  不调用 `_stream_canonical` 且 feed_objects sha256 复用；
  `test_register_detects_source_change_after_read` 锁定读后一致性检查。
- **A3**：`test_alignment_streams_each_feed_once` 断言对齐阶段仅 master 集合 +
  每 feed 一次流式读取（CAS 打开总数 3，含去重存储校验）；overlap 数值 1.0；
  `test_derive_bad_value_reports_row_number`（"row 10"）与
  `test_derive_output_size_cap` 通过。
- **A4**：`test_refresh_reuses_fingerprints_and_drops_stale_entries` 断言无变更
  二次刷新零 file_hash 调用、快照哈希不变；删除源文件后条目清空；
  `test_search_excludes_stale_entries_after_refresh` 断言搜索不再返回陈旧条目。
- **A5**：`test_state_store_reuses_connection_per_thread` 锁定每线程连接复用；
  全套件 ResourceWarning 由 2218 降至 0。
- **A6**：`test_clean_cas_preserves_referenced_objects`（引用保护 + 非哈希文件
  保留）、`test_clean_drafts_skips_referenced_drafts`（run_plan 引用保护）、
  `test_clean_approvals_deletes_used_and_expired`、CLI cas 路径测试全部通过。
- **A7**：ruff/format/mypy（27 source files，0 error）通过。
- **A8**：`PYTHONPATH=src .runtime/bin/python -m pytest -q`：
  **226 passed, 2 skipped, 1 warning**，覆盖率 **82%**（≥80 门槛）。
- **A9**：`python scripts/run_acceptance.py --matrix all --require-no-skills
  --require-no-agent` **status=passed**，14/14 格、
  `source_checkout_on_sys_path=false`；协议测试 5/5。

## 结论

通过。R1-R8 全部满足：流式注册与旧实现逐字节兼容（真实回测矩阵锁定），
源级去重与对齐流式化生效，派生管线加固，refresh 增量免重哈希且陈旧条目
清除，SQLite 连接治理消除告警洪流，三种保留清理带引用保护与审计，耗时
打点就位。缓存哈希仅用于 inspect 展示路径，完整性关键路径保持无缓存
file_hash（设计文档明示的边界）。
