# 迭代 002：验收文档

## 1. 验收用例

| ID | 场景 | 命令/操作 | 通过标准 |
| --- | --- | --- | --- |
| A1 | 受限 import | 用默认仅含 `backtrader` 的 spec 修改策略引入 `math` | validation 失败，含 `blocked_import`。 |
| A2 | 显式 import | 在 spec 中加入 `math` 后作相同修改 | validation 通过；生成 runner 的 `json/os/pathlib` 不需出现在 StrategySpec。 |
| A3 | 单模式运行 | `run_modes=["runonce"]` 配合 `runonce` profile | prepare 与实际 job 成功，manifest 的 execution modes 仅为 `runonce`。 |
| A4 | 双模式拒绝 | 同一 spec 请求 `fixed_tests` 或 comparison profile | 在 `prepare_strategy_run` 阶段以明确错误拒绝，未创建可审批 plan。 |
| A5 | 静态与回归 | Ruff、mypy、默认完整 pytest | 均通过；所有十四 scaffold 仍可验证。 |
| A6 | public runtime 回归 | 强制 PyPI `backtrader==1.9.78.123` 的完整 pytest | 全部通过，确保本轮变更不重新依赖 sibling fork。 |
| A7 | clean-room 分发 | `conda run -n base python scripts/run_acceptance.py --matrix all --require-no-skills --require-no-agent` | 构建 wheel 后从 `[test]` 安装 target；14/14、wheel tests、protocol tests、independence 均通过。 |
| A8 | 依赖证据 | 审阅 A7 JSON | `backtrader=1.9.78.123`、`mcp=2.0.0`、installed origin、source absence 和 sibling absence 全部成立。 |
| A9 | metadata 一致性 | 读取 pyproject、wheel METADATA、`product_info`、README、CONTRIBUTING、CHANGELOG | 均为 `mcp>=2.0.0,<2.1`。 |
| A10 | 冷启动观察 | 在全新 temporary target 运行 A7 的首格 | fixture 保持 90 秒 max-run 安全上限；matrix 向 worker 明确请求 60 秒运行预算，并用 75 秒观察窗口等待真实 terminal state；冷启动不在测试传入的 25 秒预算下被误判，真正超时仍由产品 timeout 终结。 |

## 2. 失败处置

- A1/A2 失败：优先检查 validator 是否把 harness import 和 strategy import 混在一起；禁止
  通过放宽全局 allowlist 绕过。
- A3/A4 失败：冻结发布；修复 plan/manifest/worker 的单一模式映射后重跑。
- A7/A8 失败：不得把依赖安装改回宿主依赖；保留 JSON 诊断并修复 wheel 或脚本。
- A9 失败：以运行中的 dependency metadata 为准同步公开说明。

## 3. 证据记录

实施完成后，在本文末尾记录命令、版本、测试数、wheel SHA-256、十四格结果和任何有意
跳过。真实外部 host UI 会话不是本迭代完成条件。

### 2026-08-02 实测结果：通过

| 覆盖项 | 命令/证据 | 结果 |
| --- | --- | --- |
| A1/A2 | `PYTHONPATH=src conda run -n base python -m pytest --no-cov tests/test_drafts_validation_changes.py tests/test_jobs_reports_recovery.py` | 60 passed；窄 allowlist 拒绝 `math`，显式声明后通过，可信 harness import 不受影响。 |
| A3/A4 | 同一回归集中的单模式实际 job 与双模式拒绝用例 | `runonce` job 成功并冻结 `execution_modes=["runonce"]`；`fixed_tests` 对单模式 spec 在 prepare 阶段拒绝且未创建 plan。 |
| A5 | `ruff check`、`ruff format --check`、隔离 `mypy src/backtrader_mcp`、`PYTHONPATH=src conda run -n base python -m pytest -q` | Ruff 通过，40 files formatted；mypy 25 source files、0 error；102 passed、3 skipped、1 warning，branch coverage 76.09%。 |
| A6 | `BACKTRADER_MCP_TEST_RUNTIME_ROOT=/tmp/backtrader-mcp-runtime.BdnNn2 PYTHONPATH=src:/tmp/backtrader-mcp-runtime.BdnNn2 conda run -n base python -m pytest -q` | PyPI `backtrader==1.9.78.123`：102 passed、3 skipped、coverage 76.09%。 |
| A7/A8 | `conda run -n base python scripts/run_acceptance.py --matrix all --require-no-skills --require-no-agent` | passed；wheel SHA-256 `7c709698196ce08724f7437ba85d7b20ebc26b66389f71bf9351bb1c7ec85e99`；14/14、wheel tests 2 passed、protocol tests 2 passed、independence passed；target 内版本为 Backtrader 1.9.78.123、MCP 2.0.0、Pandas 2.2.3、pytest 9.1.1，模块 origin 在 installed target，source/Skills/Agent 均不存在。 |
| A9 | A7 wheel metadata tests、`pyproject.toml`、`product_info` 与公开文档交叉检查 | `mcp>=2.0.0,<2.1` 一致；wheel metadata test 在独立 target 通过。 |
| A10 | A7 首格的失败诊断与从零重跑 | 初次失败显示 worker 实际收到硬编码 25 秒 run budget；仅放大 `_wait` 后又暴露 fixture 的 30 秒上限。修复为 fixture 90 秒上限、candidate 60 秒、观察 75 秒后，全新 target 14/14 通过（matrix 77.73s）。 |

有意跳过：真实外部 host UI 会话仍不属于本迭代完成条件。`tests/test_protocol_v2.py` 的本地
default 环境因不是 `mcp==2.0.0` 而跳过；它已在 A7 的 isolated target 中实际执行。
