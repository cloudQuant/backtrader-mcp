# 迭代 011：测试宇宙与发布工程验收

## 验收项

| 编号 | 检查 | 期望证据 |
| --- | --- | --- |
| A1 | 覆盖率宇宙 | feed_runtime/server 计入门槛且整树 ≥80% |
| A2 | stdio E2E | 子进程 initialize/tools/list/doctor 断言 |
| A3 | 解释器上界 | requires-python <3.14 有测试锁定 |
| A4 | 性质测试 | hypothesis 三组性质测试通过 |
| A5 | 发布 | release job 定义；CHANGELOG↔__version__ 一致性测试 |
| A6 | constraints 一致性 | pin 范围校验测试 |
| A7 | 静态质量 | ruff/format/mypy |
| A8 | 全量回归 | pytest ≥80% |
| A9 | 清洁验收 | 协议 + 14 格矩阵 |

## 已记录证据

- **A1**：feed_runtime 与 server 移出 coverage omit（仅 worker 因子进程执行
  保留 omit 并注释记录后续项）；全量回归 **244 passed, 2 skipped**，覆盖率
  **81%**（≥80 门槛）。
- **A2**：`test_stdio_transport.py` 子进程 `serve` 完成 initialize →
  notifications/initialized → tools/list → doctor 调用，断言 stdout 为合法
  JSON-RPC；发现并记录管道块缓冲行为（测试子进程设 PYTHONUNBUFFERED）。
- **A3**：`requires-python = ">=3.10,<3.14"`；
  `test_requires_python_has_an_upper_bound` 锁定。
- **A4**：`tests/test_property_security.py` 三组 hypothesis 性质测试：任意单
  字节篡改必拒（40 例）、任意 `..` 相对路径必拒（100 例）、任意 JSON 形输入
  对 StrategySpec.parse 要么有效要么稳定报错（60 例）。
- **A5**：CI 新增 tag 触发 release job（清洁验收 → 构建 wheel → 发布资产）；
  `test_changelog_latest_release_matches_package_version` 取代硬编码版本字面量。
- **A6**：`test_constraints_fall_inside_pyproject_ranges` 校验 55 个 pin 落在
  声明范围内。
- **A7**：ruff/format/mypy（27 source files，0 error）通过。
- **A8**：全量回归 244 passed，覆盖率 81%。
- **A9**：`python scripts/run_acceptance.py --matrix all --require-no-skills --require-no-agent` **status=passed**，14/14 格、`source_checkout_on_sys_path=false`。

## 结论

通过。R1-R7 全部满足：覆盖率宇宙修复后整树仍 ≥80%（feed_runtime 与 MCP
表面入池，worker 子进程覆盖合并记录为后续项）；stdio 生产传输路径有了
端到端测试；解释器上界锁定 <3.14；hypothesis 性质测试就位；tag 触发的
release job 定义；CHANGELOG/constraints 一致性检查取代硬编码字面量；CI
test job 不再重复执行验收矩阵。
