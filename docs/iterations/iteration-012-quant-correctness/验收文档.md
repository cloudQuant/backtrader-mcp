# 迭代 012：量化正确性验收

| 编号 | 检查 | 期望证据 |
| --- | --- | --- |
| A1 | 政策单一来源 | 实现容差 == 政策文件；final_value override 生效单测 |
| A2 | precomputed fail-fast | 未声明 custom line 拒绝单测 |
| A3 | analyzer 白名单 | 声明 sqn 等 → extra_metrics 出现对应指标（真实回测） |
| A4 | 运行时指纹 | run-manifest 含 commit/pandas/numpy；脏 checkout 拒绝（commit 变化） |
| A5 | OHLC 门禁 | 负价/不一致 bar 拒绝且含行号 |
| A6 | seed | manifest 冻结 seed；候选进程种子生效（复现性单测） |
| A7 | 静态质量 + 回归 + 清洁验收 | 全绿 |

## 已记录证据

- **A1**：`test_comparison_uses_policy_metric_overrides` 断言实现容差与政策
  文件一致、final_value override（rel 1e-9/abs 1e-6）生效（1e-4 差异判
  mismatch、1e-7 判 match）、profile 回显政策为单一权威。
- **A2**：`test_precomputed_ml_requires_custom_lines` 断言无自定义 line 的
  precomputed_ml spec 被拒，声明后正常建草稿；既有 14 模板与 recovery 测试
  改用带 signal 列的 ML 数据集 fixture，precomputed 单元格现以真实特征线
  回测（14 格矩阵全过）。
- **A3**：`test_analyzer_metrics_flow_into_results` 以真实回测断言声明
  sqn/vwr 后 extra_metrics 含对应指标；白名单拒绝未知 analyzer。
- **A4**：`test_seed_is_canonical_and_frozen_into_run_plans` 断言 seed 入
  规范 spec 与 run plan 冻结输入、`runtime_commit` 非空；run-manifest 的
  engine.commit 与 environment 指纹（pandas/numpy）由实现写入。
- **A5**：`test_ohlc_quality_gate_rejects_bad_rows` 断言负价与 high<low
  bar 以行号拒绝。
- **A6**：seed 经 BACKTRADER_MCP_SEED 传入候选进程，入口设置 random/numpy
  种子（`_apply_seed`）。
- **A7**：ruff/format/mypy（27 source files，0 error）；全量回归
  **250 passed, 2 skipped**，覆盖率 **81%**；清洁验收 **status=passed**、
  14/14 格、`source_checkout_on_sys_path=false`；协议测试 5/5。

## 结论

通过。R1-R6 全部满足：比较政策成为单一权威并实际生效；precomputed_ml
静默降级被 fail-fast 取代（测试矩阵转为真实特征线回测）；analyzer 白名单
接通 extra_metrics；运行时指纹升级至 commit+pandas/numpy；OHLC 数据质量
门禁带行号拒绝；seed 机制冻结并注入候选进程。
