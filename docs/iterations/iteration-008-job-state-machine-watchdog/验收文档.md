# 迭代 008：作业状态机硬化与监督闭环验收

## 验收项

| 编号 | 检查 | 期望证据 |
| --- | --- | --- |
| A1 | CAS 原语 | update 前置条件失败抛 Conflict 且不写库；delete/checkpoint 单测。 |
| A2 | 终态仲裁 | 终态/CANCEL_REQUESTED 抑制非取消终态写入；cancel 在终态后返回 already_terminal 且不杀进程；SUCCEEDED 不被覆写为 ORPHANED。 |
| A3 | watchdog | 失速/超时/孤儿三类判定单测；终态不被触碰；线程生命周期；仅在 serve 启动。 |
| A4 | 并发与幂等 | 名额满 Conflict 附建议；跨 plan 幂等碰撞无重复作业；bool timeout 拒绝。 |
| A5 | error_kind | 信号死亡=resource_limit、校验=validation、策略=user_strategy、取消=cancelled、孤儿=orphaned、崩溃兜底=infrastructure。 |
| A6 | 保留与诊断 | clean --kind jobs 只删过期终态；doctor jobs 段只读且无 state 时不创建文件。 |
| A7 | 静态质量 | ruff / format / mypy 通过。 |
| A8 | 全量回归 | pytest 通过且覆盖率 ≥80%（worker 变更经真实子进程回测）。 |
| A9 | 清洁验收 | 隔离 mcp==2.0.0 协议测试通过；14 格矩阵 status=passed。 |

## 已记录证据

- **A1、A2**：`tests/test_job_state_machine.py` 22 用例锁定 CAS 原语与仲裁：
  前置条件失败抛 Conflict 且对象不变；`_write_terminal` 在终态与
  CANCEL_REQUESTED 上被抑制；`_write_running` 仅在 QUEUED 生效；cancel 在
  SUCCEEDED 作业上返回 already_terminal 且零 kill 调用；SUCCEEDED+死 pid
  不被覆写为 ORPHANED（C2 回归）；RUNNING+死 pid 正确孤儿化并带
  error_kind=orphaned。
- **A3**：watchdog 三判定（孤儿/心跳失速/墙钟超时）与终态豁免、线程生命周期、
  候选进程组清理断言全部通过；watchdog 仅在 `run_stdio` 启动（协议测试
  `test_mcp_v2_accepts_injected_service` 验证注入服务与线程生命周期，
  CLI 构造路径无 watchdog）。
- **A4**：`_assert_capacity` 在全局锁内原子化，Conflict 附 list_jobs 建议；
  锁内幂等复查；`timeout_seconds=True` 拒绝（InvalidRequest）。
- **A5**：`_classify_exit` 纯函数测试（-9→resource_limit 含 SIGKILL、1→
  user_strategy、0→None）；各写入点 error_kind 断言（validation/
  infrastructure/cancelled/orphaned/timeout）；worker 顶层崩溃兜底。
- **A6**：`clean_jobs` 只删终态且 finished_at<before 的作业（行+目录+audit+
  WAL checkpoint）；CLI `clean --kind jobs` 路径测试；doctor jobs 段在有
  state 时输出状态计数/最老活跃作业/WAL 大小，无 state 时返回 None 且
  **不创建 state 根目录**（只读回归）。
- **A7**：`ruff check`、`ruff format --check`（51 文件）、
  `mypy src/backtrader_mcp`（27 source files，0 error）全部通过。
- **A8**：`PYTHONPATH=src .runtime/bin/python -m pytest -q`：
  **207 passed, 2 skipped, 1 warning**，分支覆盖率 **82.42%**（≥80 门槛）；
  全部 worker 变更经真实子进程回测（14 格矩阵与 jobs/recovery 测试）。
- **A9**：`python scripts/run_acceptance.py --matrix all --require-no-skills
  --require-no-agent` 最终 **status=passed**，矩阵 14/14 格通过、
  `source_checkout_on_sys_path=false`；隔离 mcp==2.0.0 协议测试 5/5 通过。

## 结论

通过。R1-R8 全部满足：CAS 原语与终态仲裁、服务器 watchdog、原子并发闸门、
error_kind 结构化、保留策略与 doctor 诊断、bool 输入加固均经单测与协议测试
锁定；全量回归、覆盖率门槛、静态质量与清洁 wheel 验收全部通过。
