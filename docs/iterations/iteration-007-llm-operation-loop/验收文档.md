# 迭代 007：LLM 操作闭环与上下文预算验收

## 验收项

| 编号 | 检查 | 期望证据 |
| --- | --- | --- |
| A1 | 快照瘦身与分页 | 默认 `get_catalog_snapshot` 响应 <1 KB 且无 `entries`；`include_entries=true` 的分页元数据正确；越界拒绝。 |
| A2 | 日志通道 | `get_run_logs` 有界/脱敏/格式校验通过单测；`get_run_status` 含 `log_uri`；日志资源经协议测试可发现。 |
| A3 | 作业枚举 | `list_jobs` 过滤（含 active）、倒序、分页、摘要形状与 CLI 一致，经单测；表面注册与注解经协议测试。 |
| A4 | 工具注解 | 协议测试断言 29 个工具全量集合，并对全部 16 个只读工具与 13 个变更工具逐一定位 readOnlyHint，破坏性工具定位 destructiveHint。 |
| A5 | 错误契约 | 协议错误注入断言 `[code]` 与 `Suggestion:` 文本（search 未知 archetype 携带 list_strategy_templates 建议）；schema 资源错误枚举允许值；无绝对路径泄漏。 |
| A6 | 搜索分页与指引 | `total/has_more/offset`、空结果指引、未知 archetype 枚举合法值，经单测。 |
| A7 | 轮询辅助 | `elapsed_seconds`/`eta_bound` 派生字段单测；`run_backtest` prompt 轮询节奏。 |
| A8 | 静态质量 | `ruff check`、`ruff format --check`、`mypy` 通过。 |
| A9 | 全量回归 | `pytest -q` 通过且覆盖率 ≥80%。 |
| A10 | 协议与清洁验收 | 隔离 mcp==2.0.0 协议测试通过；14 格矩阵与独立性审计通过。 |

## 已记录证据

- **A1-A3、A6、A7**：新增 `tests/test_errors_boundary.py`（8 用例）、
  `tests/test_jobs_observability.py`（8 用例）、`tests/test_catalog_snapshot.py`
  分页与指引用例（6 新增/更新）、`tests/test_data_contracts.py` 截断指引断言。
  默认快照响应实测 340 字节（<1 KB），`extensions.entry_count=1155`；分页遍历
  1155 条无重复；日志读取实测绝对路径替换为 `<path>`、`../` 穿越 job_id 被拒。
- **A4、A5**：隔离 mcp==2.0.0 环境（`.runtime` venv，constraints 固定）协议测试
  4/4 通过：29 工具/4 具体资源/6 模板资源/8 prompt 全量集合断言；16 只读 + 13
  变更工具的 readOnlyHint 全量定位；破坏性工具 destructiveHint；错误注入断言
  （`[invalid_request]` + "between 1 and"；未知 archetype 错误含 `Suggestion:`
  与 list_strategy_templates 指引）；`contracts/nope` 错误消息含 "allowed
  values" 与 "strategy-spec"；`jobs/{id}/logs` 资源实际读取并断言绝对路径
  脱敏（`<path>` 替换生效）。
- **A8**：`ruff check src tests scripts` 通过；`ruff format --check` 50 文件全部
  符合；`mypy src/backtrader_mcp`（27 source files）0 error；
  `audit-independence` status=passed、files_checked=27、findings=[]。
- **A9**：`PYTHONPATH=src .runtime/bin/python -m pytest -q`：
  **181 passed, 2 skipped, 1 warning**（skip 为需要显式 wheel 路径的
  wheel 验收用例），分支覆盖率 **81.23%**（≥80 门槛）。
- **A10**：`python scripts/run_acceptance.py --matrix all --require-no-skills
  --require-no-agent` 最终 **status=passed**；矩阵 14/14 格通过；
  `source_checkout_on_sys_path=false`（distribution 与矩阵执行环境两处）；
  `origin_is_expected_installed_target=true`；独立性审计 passed；
  sibling 检查 agent/skills 均 absent；隔离 target 中
  `backtrader=1.3.0`、`repository=github.com/cloudquant/backtrader`、
  `trusted=true`。
- **附加修复（超出原范围的必需变更）**：嵌套 venv 溯源误判 bug——`git -C` 探测
  发现外层产品仓库导致已安装包 runtime 被误拒（CONTRIBUTING 推荐的 `.runtime`
  布局必然触发）。修复后 `inspect_runtime_root` 对"注册根 == 已安装包根"优先采用
  发行版溯源；回归测试
  `test_installed_root_inside_foreign_checkout_uses_distribution_provenance` 与
  `test_unrelated_checkout_root_is_still_rejected` 双向锁定。
- **验收过程发现的环境问题（非代码缺陷）**：`.runtime` venv 使用 editable 安装
  会把 `src` 注入 `sys.path` 导致清洁验收的独立检查失败；改用非 editable 安装
  （`pip install -c constraints .`，开发时 `PYTHONPATH=src`）后通过。已按
  README 的既有开发流程执行。

## 结论

通过。R1-R8 全部满足：快照默认瘦身到 340 字节并支持分页；日志通道有界且脱敏；
作业可枚举；29 工具注解与结构化错误契约经真实协议验证；搜索分页与空结果指引
就位；轮询辅助字段与 prompt 节奏明确；全量回归、覆盖率门槛、静态质量与清洁
wheel 14 格验收全部通过。
