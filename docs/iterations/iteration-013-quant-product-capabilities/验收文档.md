# 迭代 013：量化产品能力与收尾验收

| 编号 | 检查 | 期望证据 |
| --- | --- | --- |
| A1 | derive 管道 | 派生列可达（真实回测 precomputed_ml 用派生特征） |
| A2 | 参数扫描 | 冻结网格 + 逐组合结果 + 排名（真实回测） |
| A3 | alignment | left/explicit_asof 拒绝 |
| A4 | 模板与文档 | notify_order/sizer 模板生成 + 执行语义 README |
| A5 | MT5 | 秒级 timeframe 拒绝 |
| A6 | list_target_tree | 协议测试含新工具（30 工具） |
| A7 | 覆盖率通道 | COVERAGE_PROCESS_START 透传 |
| A8 | 静态+回归+清洁验收 | 全绿 |

## 已记录证据

- **A1**：derive 丢弃 warmup 行并以 pandas_custom_lines 声明派生列；
  `test_parameter_sweep_*` 与真实回测验证参数网格；precomputed_ml 可用
  派生特征（012 fail-fast 后的管道闭环）。
- **A2**：`test_parameter_sweep_freezes_grid_and_ranks_combinations` 真实
  回测 4 组合，断言冻结网格、组合计数、按 return_rate 降序排名；非法参数名
  与缺失网格拒绝。
- **A3**：alignment 仅接受 intersection（实现拒绝 left/explicit_asof）。
- **A4**：模板生成含 notify_order 与 record_fills（默认惰性）；README 增加
  执行语义段（sizer/佣金/无风险利率/drawdown 符号/yahoo adjclose/bar2edge/
  sweep 语义）。
- **A5**：mt5_csv 拒绝 ticks/microseconds/seconds timeframe。
- **A6**：`list_target_tree` 只读工具（协议测试全量集合 30 工具）+
  `test_list_target_tree_reads_confined_tree`。
- **A7**：COVERAGE_PROCESS_START 经 worker 环境透传。
- **A8**：ruff（CI 固定 0.16.3）/format/mypy 全绿；全量回归 **253 passed,
  2 skipped**，覆盖率 **81%**；清洁验收 **status=passed**、14/14 格、
  含 stdio 传输 E2E 与协议测试；`source_checkout_on_sys_path=false`。
- **审查修复（随本迭代合并）**：CI 红灯（runtime_commit 环境无关化 + ruff
  版本 pin）；iat 过去签发按 exp 治理；nonce 消费移至持久结果之后；OHLC
  门禁 opt-out；sma deque；clean nonces；版本字面量移除；stdio E2E 进入
  验收管线。

## 结论

通过。R1-R7 全部满足，并顺带修复了 009-012 合并后审查的全部 CRITICAL/HIGH
与主要 MEDIUM（master CI 由红灯转绿）。
