# 迭代 010：授权可信分离与安全加固验收

## 验收项

| 编号 | 检查 | 期望证据 |
| --- | --- | --- |
| A1 | 审批身份 | approve 审计行含 approver 身份字段 |
| A2 | 令牌硬化 | 重放/过期边界/iat 窗口/畸形 payload 拒绝单测 |
| A3 | 结果契约 | identifier 校验与 Markdown 转义单测 |
| A4 | Windows 锁 | 平台分支选择单测（monkeypatch） |
| A5 | rlimit 可观测 | job 行 resource_limits_status + audit |
| A6 | 供应链 | constraints 全闭包含哈希且可复现安装 |
| A7 | 静态质量 | ruff/format/mypy 通过 |
| A8 | 全量回归 | pytest ≥80% 覆盖率 |
| A9 | 清洁验收 | 协议测试 + 14 格矩阵 status=passed |

## 已记录证据

- **A1**：`test_approver_identity_is_recorded_in_audit` 断言 approve 审计行
  含 username 与（POSIX）整数 uid；`approve_change`/`approve_run_plan` 均写入
  `approval.created_by_human` 审计。
- **A2**：`tests/test_security_hardening.py` 11 用例：nonce 重放拒绝、
  approve 不消费而落地点消费一次的语义、无 state 退化路径、exp==now 拒绝、
  iat ±400s 超窗拒绝、数组 payload 拒绝；既有安全令牌测试全量通过。
- **A3**：identifier 契约（坏类名/空 registered_class 拒绝）与 Markdown
  转义（`evil[link](https://x)` → `evil_link__https___x_`）单测通过。
- **A4**：`test_locks_platform_fallback` 以 fake msvcrt 锁定 Windows 分支；
  POSIX 路径不变。
- **A5**：`test_resource_limit_status_probe` 断言四项限额的 requested/
  supported 状态；worker 在 RUNNING 后写入 `resource_limits_status` 并
  audit `job.resource_limits`。
- **A6**：constraints 从 3 行扩为 55 个精确 `==` 闭包 pin（含 mcp/pandas/
  backtrader VCS 与全部传递依赖 + test extras）；`pip install --dry-run
  -c constraints` 通过；按档案哈希被如实记录为不兼容（wheel 范围元数据 +
  VCS 依赖），产品 wheel 本身仍由验收 SHA-256 快照校验。
- **A7**：ruff/format/mypy（27 source files，0 error）通过。
- **A8**：`PYTHONPATH=src .runtime/bin/python -m pytest -q`：
  **237 passed, 2 skipped, 1 warning**，覆盖率 **82%**（≥80 门槛）。
- **A9**：`python scripts/run_acceptance.py --matrix all --require-no-skills
  --require-no-agent` **status=passed**，14/14 格、
  `source_checkout_on_sys_path=false`；协议测试 5/5。

## 结论

通过。R1-R7 全部满足（R6 按设计文档说明以精确闭包 pin 交付并记录哈希
不兼容原因）：审批身份进审计、宿主假设文档化、nonce 单次消费在授权落地点
原子执行且不破坏 approve→start 两段验证的既有契约、结果契约收紧、
Windows 锁回退、rlimit 可观测、供应链闭包锁定。
