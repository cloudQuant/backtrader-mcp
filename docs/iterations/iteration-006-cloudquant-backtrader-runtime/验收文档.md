# 迭代 006：CloudQuant Backtrader 运行时来源限制验收

## 验收项

| 编号 | 检查 | 期望证据 |
| --- | --- | --- |
| A1 | 来源策略单测 | CloudQuant Git/PEP 610 通过；其他来源拒绝；缺失安装与不覆盖警告均覆盖。 |
| A2 | Job 边界 | `prepare_strategy_run` 拒绝不可信 runtime。 |
| A3 | Doctor/CLI | `doctor` 输出可信来源字段及不可信安装 warning；`install-backtrader` 的当前环境行为可复现。 |
| A4 | 静态质量 | Ruff、格式检查、mypy 均通过。 |
| A5 | 全量回归 | `pytest -q` 通过且覆盖率不低于 80%。 |
| A6 | 清洁 wheel | VCS 安装、来源、14/14 矩阵、协议测试、wheel 测试与独立性审计全部通过。 |

## 已记录证据

- A1、A2、A3：全量测试覆盖 CloudQuant Git/PEP 610 来源、非可信安装 warning、缺失安装
  命令、默认 runtime 发现，以及 prepare/start 的双重来源校验。当前解释器实测
  `python -m backtrader_mcp install-backtrader` 返回 `action=already_installed`、
  `trusted=true`、`repository=github.com/cloudquant/backtrader`，未发生安装或覆盖。
- A3：隔离 target 的 `doctor` 实测为 `status=passed`，自动发现的 `default` runtime 为
  `backtrader=1.3.0`、`trusted_source=true`；不可信 runtime 在探测前即报告
  `runtime_untrusted_source`，不导入其代码。
- A4：`ruff check .`、`ruff format --check .` 与在 `mcp==2.0.0` 隔离 target 上执行的
  `mypy src/backtrader_mcp` 均通过（27 source files，0 error）。
- A5：`PYTHONPATH=src conda run -n base python -m pytest -q`：151 passed、3 skipped、
  1 expected warning，coverage 80.60%。
- A6：`conda run -n base python scripts/run_acceptance.py --matrix all --require-no-skills
  --require-no-agent`：最终状态 `passed`；wheel 干净 target 安装
  `backtrader=1.3.0`、`mcp=2.0.0`，PEP 610 provenance 为 `direct_url_vcs`，
  `repository=github.com/cloudquant/backtrader`、`trusted=true`；14/14 矩阵、2 个
  wheel 测试、2 个协议测试及独立性审计全部通过。

## 结论

通过。产品依赖、已安装包诊断、runtime 注册、策略运行边界和清洁 wheel 验收均已限制为
`cloudQuant/backtrader`；其他已安装 Backtrader 不会被静默替换，而会得到机器可读 warning。
